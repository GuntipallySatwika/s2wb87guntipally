extends layout

block content
  h1= title
  - let image_names=["1.jpg","2.jpg","3.jpg","4.jpg","5.jpg"]
  - for(let j=0;j<3;j++) {
     - let r= Math.floor(Math.random()*5)
     img(src=`/images/${image_names[r]}` alt=`Image No ${r}` height="300px" width="300px")
  - }